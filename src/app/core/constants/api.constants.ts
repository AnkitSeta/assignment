import { environment } from 'src/environments/environment';

const API = {
  BASE_URL: `${environment.baseUrl}/api`,
};


export const API_END_POINTS = {
  STUDENTS_RESULT: {
    get: './assets/mis/data.json'
  }
}